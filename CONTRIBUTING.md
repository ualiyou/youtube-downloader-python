# Contributing

Open an issue before a large change. Small focused pull requests are welcome.

Run the checks before submitting:

```bash
python -m unittest discover -s tests -v
```

Do not include downloaded media, cookies, or private URLs in issues or pull requests.
